import { PulseLoader } from "react-spinners";

export default function PulseSpinner() {
  return (
    <PulseLoader
      color="#0d455fbf"
      margin={12}
      size={12}
      speedMultiplier={0.8}
    />
  );
}
